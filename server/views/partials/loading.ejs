<!DOCTYPE html>
<html lang="en">
<head>
    <!-- Add your head content here -->
    <style>
        /* Existing styles */
        body {
            margin: 0;
            overflow: hidden;
            font-family: Arial, sans-serif;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            background-color: #f0f0f0; /* Set your background color */
        }

        .loading-screen {
            text-align: center;
            padding: 20px;
            background-color: #fff; /* Set your loading screen background color */
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        .loading-spinner {
            border: 4px solid rgba(0, 0, 0, 0.1);
            border-radius: 50%;
            border-top: 4px solid #3498db; /* Set your spinner color */
            width: 40px;
            height: 40px;
            animation: spin 1s linear infinite;
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        /* Hide the loading screen once the content is loaded */
        body.loaded .loading-screen {
            animation: fadeOut 1s ease-out;
            animation-fill-mode: forwards;
        }

        @keyframes fadeOut {
            0% { opacity: 1; }
            100% { opacity: 0; }
        }

        /* Masking div styles */
        .masking-div {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(255, 255, 255, 0.8); /* Semi-transparent white */
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 10;
        }

        .hide{
            display: none;
        }
    </style>
    <script>
        // Simulate content loaded event
        window.addEventListener('load', function () {
            // Wait for 3 seconds and then hide the masking div
            setTimeout(() => {
                document.querySelector('.masking-div').style.display = 'none';
            }, 3000);
        });
    </script>
</head>
<body>
    <!-- Masking div for overlay -->
    <div class="masking-div hide">

    </div>


    <!-- Your Home Page Content Goes Here -->

</body>
</html>
